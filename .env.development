# Overrides .env for `npm run dev` only.
# Route through the Vite dev server proxy (see vite.config.ts) instead of
# calling the deployed backend directly, so the browser never sees a
# cross-origin request and CORS doesn't apply.
VITE_API_BASE_URL=/api
